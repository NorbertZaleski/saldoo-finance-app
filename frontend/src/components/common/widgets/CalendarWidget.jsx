import Widget from '../Widget';

const CalendarWidget = ({ data }) => {
  return (
    <Widget size="small">
        <Widget.Body>
            <span>calendar</span>
        </Widget.Body>
    </Widget>
  );
};

export default CalendarWidget;